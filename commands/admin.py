import discord\nfrom discord import app_commands, ui\nimport json\nimport os\nfrom datetime import datetime\n\nclass AdminPanel:\n    \"\"\"Système administrateur pour gérer le jeu sans toucher au code\"\"\"\n    \n    def __init__(self, bot):\n        self.bot = bot\n        self.data_path = \"data/\"\n    \n    def load_json(self, filename):\n        \"\"\"Charge un fichier JSON\"\"\"\n        filepath = os.path.join(self.data_path, filename)\n        try:\n            with open(filepath, 'r', encoding='utf-8') as f:\n                return json.load(f)\n        except FileNotFoundError:\n            return None\n    \n    def save_json(self, filename, data):\n        \"\"\"Sauvegarde un fichier JSON\"\"\"\n        filepath = os.path.join(self.data_path, filename)\n        with open(filepath, 'w', encoding='utf-8') as f:\n            json.dump(data, f, indent=2, ensure_ascii=False)\n        return True\n\n# ========================= COMMANDES ADMIN =========================\n\ndef setup_admin_commands(tree, admin_panel):\n    \"\"\"Configure toutes les commandes admin\"\"\"\n    \n    # ========== GROUPE UTILISATEUR ==========\n    @tree.command(name=\"admin_user\", description=\"Gérer les joueurs\")\n    @app_commands.describe(\n        action=\"Voir / Donner XP / Donner Coins / Définir Rang / Bannir\",\n        user=\"L'utilisateur cible\",\n        amount=\"Montant (pour XP/Coins)\",\n        rank=\"Numéro du rang (1-11)\"\n    )\n    async def admin_user(interaction: discord.Interaction, action: str, user: discord.User, amount: int = 0, rank: int = 0):\n        \"\"\"Commande d'administration utilisateur\"\"\"\n        \n        # Vérification admin\n        if not interaction.user.guild_permissions.administrator:\n            return await interaction.response.send_message(\"❌ Tu n'as pas les permissions admin!\", ephemeral=True)\n        \n        players = admin_panel.load_json('players.json')\n        if players is None:\n            players = {}\n        \n        user_id = str(user.id)\n        \n        if action.lower() == \"view\" or action.lower() == \"voir\":\n            if user_id not in players:\n                return await interaction.response.send_message(f\"❌ {user.mention} n'a pas de profil!\", ephemeral=True)\n            \n            player = players[user_id]\n            embed = discord.Embed(title=f\"📊 Profil Admin - {user.name}\", color=0xFF0000)\n            embed.add_field(name=\"Level\", value=player.get('level', 1), inline=True)\n            embed.add_field(name=\"XP\", value=player.get('xp', 0), inline=True)\n            embed.add_field(name=\"Coins\", value=player.get('coins', 0), inline=True)\n            embed.add_field(name=\"Rang\", value=player.get('rank', 'Unknown'), inline=False)\n            embed.add_field(name=\"Souffle\", value=player.get('breathing', 'Not unlocked'), inline=False)\n            embed.add_field(name=\"Missions Complétées\", value=player.get('missions_completed', 0), inline=True)\n            embed.add_field(name=\"Démons Tués\", value=player.get('demon_kills', 0), inline=True)\n            embed.set_thumbnail(url=user.display_avatar.url)\n            \n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        elif action.lower() == \"give_xp\" or action.lower() == \"donner_xp\":\n            if user_id not in players:\n                players[user_id] = {\n                    'level': 1,\n                    'xp': 0,\n                    'coins': 0,\n                    'rank': 'Apprentice Slayer',\n                    'breathing': 'Not unlocked',\n                    'missions_completed': 0,\n                    'demon_kills': 0\n                }\n            \n            players[user_id]['xp'] += amount\n            admin_panel.save_json('players.json', players)\n            \n            embed = discord.Embed(\n                title=\"✅ XP Donné\",\n                description=f\"{user.mention} a reçu **+{amount} XP**\",\n                color=0x00FF00\n            )\n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        elif action.lower() == \"give_coins\" or action.lower() == \"donner_coins\":\n            if user_id not in players:\n                players[user_id] = {\n                    'level': 1,\n                    'xp': 0,\n                    'coins': 0,\n                    'rank': 'Apprentice Slayer',\n                    'breathing': 'Not unlocked',\n                    'missions_completed': 0,\n                    'demon_kills': 0\n                }\n            \n            players[user_id]['coins'] += amount\n            admin_panel.save_json('players.json', players)\n            \n            embed = discord.Embed(\n                title=\"✅ Coins Donnés\",\n                description=f\"{user.mention} a reçu **+{amount} 🪙**\",\n                color=0x00FF00\n            )\n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        elif action.lower() == \"set_rank\" or action.lower() == \"definir_rang\":\n            ranks = admin_panel.load_json('ranks.json')\n            if ranks is None:\n                return await interaction.response.send_message(\"❌ Erreur lors du chargement des rangs!\", ephemeral=True)\n            \n            if rank < 1 or rank > len(ranks['ranks']):\n                return await interaction.response.send_message(f\"❌ Rang invalide! (1-{len(ranks['ranks'])})\" , ephemeral=True)\n            \n            rank_name = ranks['ranks'][rank-1]['name']\n            \n            if user_id not in players:\n                players[user_id] = {\n                    'level': 1,\n                    'xp': 0,\n                    'coins': 0,\n                    'rank': rank_name,\n                    'breathing': 'Not unlocked',\n                    'missions_completed': 0,\n                    'demon_kills': 0\n                }\n            else:\n                players[user_id]['rank'] = rank_name\n            \n            admin_panel.save_json('players.json', players)\n            \n            embed = discord.Embed(\n                title=\"✅ Rang Changé\",\n                description=f\"{user.mention} est maintenant **{rank_name}**\",\n                color=0x00FF00\n            )\n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        elif action.lower() == \"ban\" or action.lower() == \"bannir\":\n            if user_id not in players:\n                players[user_id] = {\n                    'level': 1,\n                    'xp': 0,\n                    'coins': 0,\n                    'rank': 'Apprentice Slayer',\n                    'breathing': 'Not unlocked',\n                    'missions_completed': 0,\n                    'demon_kills': 0,\n                    'banned': True,\n                    'ban_reason': 'Admin ban'\n                }\n            else:\n                players[user_id]['banned'] = True\n                players[user_id]['ban_reason'] = 'Admin ban'\n            \n            admin_panel.save_json('players.json', players)\n            \n            embed = discord.Embed(\n                title=\"🚫 Joueur Banni\",\n                description=f\"{user.mention} a été **banni** du jeu!\",\n                color=0xFF0000\n            )\n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        else:\n            await interaction.response.send_message(\"❌ Action non reconnue!\", ephemeral=True)\n    \n    # ========== GROUPE ÉVÉNEMENTS ==========\n    @tree.command(name=\"admin_event\", description=\"Gérer les événements\")\n    @app_commands.describe(\n        action=\"create / list / end\",\n        event_type=\"xp_double / coin_boost / xp_triple / demon_rare\",\n        duration=\"Durée en heures (optionnel)\"\n    )\n    async def admin_event(interaction: discord.Interaction, action: str, event_type: str = None, duration: int = 24):\n        \"\"\"Commande d'administration événements\"\"\"\n        \n        # Vérification admin\n        if not interaction.user.guild_permissions.administrator:\n            return await interaction.response.send_message(\"❌ Tu n'as pas les permissions admin!\", ephemeral=True)\n        \n        events = admin_panel.load_json('events.json')\n        if events is None:\n            return await interaction.response.send_message(\"❌ Erreur lors du chargement des événements!\", ephemeral=True)\n        \n        if action.lower() == \"create\":\n            if event_type is None:\n                return await interaction.response.send_message(\"❌ Tu dois spécifier le type d'événement!\", ephemeral=True)\n            \n            # Vérifier si l'événement existe\n            event = next((e for e in events['available_events'] if e['id'] == event_type), None)\n            if event is None:\n                return await interaction.response.send_message(f\"❌ Événement '{event_type}' non trouvé!\", ephemeral=True)\n            \n            new_event = {\n                'id': event_type,\n                'name': event['name'],\n                'emoji': event['emoji'],\n                'multiplier': event['multiplier'],\n                'type': event['type'],\n                'started_at': datetime.now().isoformat(),\n                'duration_hours': duration\n            }\n            \n            events['active_events'].append(new_event)\n            admin_panel.save_json('events.json', events)\n            \n            embed = discord.Embed(\n                title=\"✅ Événement Créé\",\n                description=f\"{event['emoji']} **{event['name']}** est activé pour {duration}h!\",\n                color=0x00FF00\n            )\n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        elif action.lower() == \"list\":\n            if not events['active_events']:\n                return await interaction.response.send_message(\"❌ Aucun événement actif!\", ephemeral=True)\n            \n            embed = discord.Embed(\n                title=\"📋 Événements Actifs\",\n                color=0x00FF00\n            )\n            \n            for event in events['active_events']:\n                embed.add_field(\n                    name=f\"{event['emoji']} {event['name']}\",\n                    value=f\"Multiplicateur: {event['multiplier']}x\\nDurée: {event['duration_hours']}h\",\n                    inline=False\n                )\n            \n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        elif action.lower() == \"end\":\n            if event_type is None:\n                return await interaction.response.send_message(\"❌ Tu dois spécifier l'ID de l'événement à arrêter!\", ephemeral=True)\n            \n            initial_count = len(events['active_events'])\n            events['active_events'] = [e for e in events['active_events'] if e['id'] != event_type]\n            \n            if len(events['active_events']) == initial_count:\n                return await interaction.response.send_message(f\"❌ Événement '{event_type}' non trouvé!\", ephemeral=True)\n            \n            admin_panel.save_json('events.json', events)\n            \n            embed = discord.Embed(\n                title=\"✅ Événement Arrêté\",\n                description=f\"L'événement **{event_type}** a été arrêté!\",\n                color=0x00FF00\n            )\n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        else:\n            await interaction.response.send_message(\"❌ Action non reconnue!\", ephemeral=True)\n    \n    # ========== GROUPE CONFIG ==========\n    @tree.command(name=\"admin_config\", description=\"Gérer la configuration du jeu\")\n    @app_commands.describe(\n        action=\"reload / stats / reset_players\",\n        confirm=\"Confirmation pour les actions dangereuses\"\n    )\n    async def admin_config(interaction: discord.Interaction, action: str, confirm: bool = False):\n        \"\"\"Commande d'administration configuration\"\"\"\n        \n        # Vérification admin\n        if not interaction.user.guild_permissions.administrator:\n            return await interaction.response.send_message(\"❌ Tu n'as pas les permissions admin!\", ephemeral=True)\n        \n        if action.lower() == \"reload\":\n            # Vérifier que tous les fichiers JSON sont valides\n            files_to_check = [\n                'respirations.json',\n                'demons.json',\n                'ranks.json',\n                'missions.json',\n                'events.json',\n                'players.json'\n            ]\n            \n            errors = []\n            for file in files_to_check:\n                data = admin_panel.load_json(file)\n                if data is None:\n                    errors.append(f\"❌ {file} - ERREUR DE CHARGEMENT\")\n                else:\n                    errors.append(f\"✅ {file} - OK\")\n            \n            embed = discord.Embed(\n                title=\"🔄 Rechargement Configuration\",\n                color=0x00FF00\n            )\n            for error in errors:\n                embed.add_field(name=\"Status\", value=error, inline=False)\n            \n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        elif action.lower() == \"stats\":\n            players = admin_panel.load_json('players.json')\n            demons = admin_panel.load_json('demons.json')\n            missions = admin_panel.load_json('missions.json')\n            \n            total_players = len(players) if players else 0\n            total_demons = len(demons.get('upper_moons', [])) + len(demons.get('lower_moons', [])) + len(demons.get('custom_demons', [])) if demons else 0\n            total_missions = len(missions.get('missions', [])) if missions else 0\n            \n            embed = discord.Embed(\n                title=\"📊 Statistiques du Serveur\",\n                color=0x00FF00\n            )\n            embed.add_field(name=\"Joueurs Enregistrés\", value=total_players, inline=True)\n            embed.add_field(name=\"Démons Disponibles\", value=total_demons, inline=True)\n            embed.add_field(name=\"Missions Disponibles\", value=total_missions, inline=True)\n            embed.set_footer(text=f\"Dernière mise à jour: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\")\n            \n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        elif action.lower() == \"reset_players\":\n            if not confirm:\n                embed = discord.Embed(\n                    title=\"⚠️ ATTENTION\",\n                    description=\"Cette action est **IRRÉVERSIBLE**!\\n\\nTape la commande à nouveau avec `confirm:true`\",\n                    color=0xFF0000\n                )\n                return await interaction.response.send_message(embed=embed, ephemeral=True)\n            \n            admin_panel.save_json('players.json', {})\n            \n            embed = discord.Embed(\n                title=\"✅ Réinitialisation\",\n                description=\"Tous les profils des joueurs ont été supprimés!\",\n                color=0xFF0000\n            )\n            await interaction.response.send_message(embed=embed, ephemeral=True)\n        \n        else:\n            await interaction.response.send_message(\"❌ Action non reconnue!\", ephemeral=True)\n\nreturn setup_admin_commands\n