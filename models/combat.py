import json\nimport os\nimport random\nfrom enum import Enum\nfrom typing import Dict, List, Tuple\nfrom models.player import Player\n\nclass CombatAction(Enum):\n    \"\"\"Actions disponibles au combat\"\"\"\n    BASIC_ATTACK = 1\n    TECHNIQUE = 2\n    DEFEND = 3\n    ITEM = 4\n    ESCAPE = 5\n\nclass Demon:\n    \"\"\"Classe représentant un démon\"\"\"\n    \n    def __init__(self, demon_data: dict):\n        self.name = demon_data.get('name', 'Unknown Demon')\n        self.rank = demon_data.get('rank', 'Common')\n        self.hp = demon_data.get('hp', 100)\n        self.max_hp = demon_data.get('hp', 100)\n        self.atk = demon_data.get('atk', 10)\n        self.def_ = demon_data.get('def', 5)\n        self.xp_reward = demon_data.get('xp_reward', 50)\n        self.gold_reward = demon_data.get('gold_reward', 75)\n        self.image_url = demon_data.get('image_url', '')\n        self.description = demon_data.get('description', '')\n    \n    def is_alive(self) -> bool:\n        \"\"\"Vérifier si le démon est vivant\"\"\"\n        return self.hp > 0\n    \n    def take_damage(self, damage: int):\n        \"\"\"Le démon prend des dégâts\"\"\"\n        self.hp = max(0, self.hp - damage)\n    \n    def get_defense_value(self) -> int:\n        \"\"\"Obtenir la valeur de défense\"\"\"\n        return self.def_\n    \n    def to_dict(self) -> dict:\n        return {\n            'name': self.name,\n            'rank': self.rank,\n            'hp': self.hp,\n            'max_hp': self.max_hp,\n            'atk': self.atk,\n            'def': self.def_,\n            'xp_reward': self.xp_reward,\n            'gold_reward': self.gold_reward\n        }\n\nclass CombatState:\n    \"\"\"Gère l'état d'un combat\"\"\"\n    \n    def __init__(self, player: Player, demon: Demon):\n        self.player = player\n        self.demon = demon\n        self.turn = 0\n        self.max_turns = 20\n        self.player_defended = False\n        self.demon_defended = False\n        self.combat_log = []\n        self.player_hp_start = player.hp\n        self.demon_hp_start = demon.max_hp\n    \n    def is_combat_over(self) -> Tuple[bool, str]:\n        \"\"\"Vérifier si le combat est terminé. Retourne (is_over, result)\"\"\"\n        if not self.player.is_alive():\n            return True, \"PLAYER_DEFEATED\"\n        if not self.demon.is_alive():\n            return True, \"PLAYER_VICTORY\"\n        if self.turn >= self.max_turns:\n            return True, \"TIME_OUT\"\n        return False, \"\"\n    \n    def calculate_damage(self, attacker_atk: int, defender_def: int, action_multiplier: float = 1.0) -> int:\n        \"\"\"Calculer les dégâts infligés\"\"\"\n        base_damage = max(1, attacker_atk - (defender_def // 2))\n        variance = random.randint(-3, 3)\n        final_damage = int((base_damage + variance) * action_multiplier)\n        return max(1, final_damage)\n    \n    def add_log(self, message: str):\n        \"\"\"Ajouter une ligne au journal de combat\"\"\"\n        self.combat_log.append(f\"**Tour {self.turn}:** {message}\")\n    \n    def get_combat_log(self) -> str:\n        \"\"\"Obtenir le journal complet du combat\"\"\"\n        return \"\\n\".join(self.combat_log[-10:]) if self.combat_log else \"Le combat vient de commencer...\"\n\nclass CombatEngine:\n    \"\"\"Moteur de combat principal\"\"\"\n    \n    def __init__(self, data_path: str = \"data/\"):\n        self.data_path = data_path\n        self.demons_file = os.path.join(data_path, 'demons.json')\n        self.respirations_file = os.path.join(data_path, 'respirations.json')\n        self.active_combats: Dict[int, CombatState] = {}  # user_id -> CombatState\n    \n    def load_demon_data(self) -> dict:\n        \"\"\"Charger les données des démons\"\"\"\n        try:\n            with open(self.demons_file, 'r', encoding='utf-8') as f:\n                return json.load(f)\n        except FileNotFoundError:\n            return {}\n    \n    def load_breathing_data(self) -> dict:\n        \"\"\"Charger les données des souffles\"\"\"\n        try:\n            with open(self.respirations_file, 'r', encoding='utf-8') as f:\n                return json.load(f)\n        except FileNotFoundError:\n            return {}\n    \n    def get_random_demon(self, difficulty: str = \"easy\") -> Demon:\n        \"\"\"Obtenir un démon aléatoire selon la difficulté\"\"\"\n        demon_data = self.load_demon_data()\n        \n        # Filtrer les démons par difficulté\n        available_demons = []\n        \n        if difficulty == \"easy\":\n            available_demons = demon_data.get('lower_moons', [])[:3]  # Lower Moons 4-6\n            available_demons += demon_data.get('custom_demons', [])[:10]  # Premiers custom\n        elif difficulty == \"hard\":\n            available_demons = demon_data.get('lower_moons', [])  # Tous les Lower Moons\n            available_demons += demon_data.get('upper_moons', [])[-3:]  # Upper 4-6\n            available_demons += demon_data.get('custom_demons', [])[10:20]  # Customs moyens\n        elif difficulty == \"hell\":\n            available_demons = demon_data.get('upper_moons', [])  # Tous les Upper Moons\n            available_demons += demon_data.get('custom_demons', [])[15:]  # Customs forts\n        \n        if not available_demons:\n            # Fallback: prendre n'importe quel démon\n            all_demons = (demon_data.get('lower_moons', []) + \n                         demon_data.get('upper_moons', []) + \n                         demon_data.get('custom_demons', []))\n            available_demons = all_demons\n        \n        return Demon(random.choice(available_demons))\n    \n    def get_specific_demon(self, demon_name: str) -> Demon:\n        \"\"\"Obtenir un démon spécifique\"\"\"\n        demon_data = self.load_demon_data()\n        \n        # Chercher dans Upper Moons\n        for demon in demon_data.get('upper_moons', []):\n            if demon['name'].lower() == demon_name.lower():\n                return Demon(demon)\n        \n        # Chercher dans Lower Moons\n        for demon in demon_data.get('lower_moons', []):\n            if demon['name'].lower() == demon_name.lower():\n                return Demon(demon)\n        \n        # Chercher dans Custom Demons\n        for demon in demon_data.get('custom_demons', []):\n            if demon['name'].lower() == demon_name.lower():\n                return Demon(demon)\n        \n        # Chercher Muzan\n        if demon_name.lower() == \"muzan\":\n            muzan = demon_data.get('muzan')\n            if muzan:\n                return Demon(muzan)\n        \n        # Fallback: retourner un démon aléatoire\n        return self.get_random_demon()\n    \n    def start_combat(self, user_id: int, player: Player, demon: Demon) -> CombatState:\n        \"\"\"Commencer un nouveau combat\"\"\"\n        combat_state = CombatState(player, demon)\n        self.active_combats[user_id] = combat_state\n        return combat_state\n    \n    def get_combat(self, user_id: int) -> CombatState:\n        \"\"\"Obtenir le combat actif d'un joueur\"\"\"\n        return self.active_combats.get(user_id)\n    \n    def end_combat(self, user_id: int) -> CombatState:\n        \"\"\"Terminer le combat et le supprimer de la liste active\"\"\"\n        combat = self.active_combats.pop(user_id, None)\n        return combat\n    \n    def player_action(self, user_id: int, action: CombatAction, technique_index: int = 0) -> Tuple[bool, dict]:\n        \"\"\"Gérer une action du joueur. Retourne (is_over, result_data)\"\"\"\n        combat = self.get_combat(user_id)\n        if not combat:\n            return False, {'error': 'Aucun combat actif'}\n        \n        result = {'player_action': '', 'demon_action': '', 'player_hp': combat.player.hp, 'demon_hp': combat.demon.hp}\n        combat.turn += 1\n        combat.player_defended = False\n        \n        # Action du joueur\n        if action == CombatAction.BASIC_ATTACK:\n            damage = self.calculate_damage(combat.player.atk, combat.demon.get_defense_value())\n            combat.demon.take_damage(damage)\n            result['player_action'] = f\"⚔️ Attaque Basique! **{damage}** dégâts infligés\"\n            combat.add_log(f\"{combat.player.username} a utilisé Attaque Basique ({damage} dmg)\")\n        \n        elif action == CombatAction.TECHNIQUE:\n            # Utiliser une technique du souffle\n            if technique_index < len(combat.player.breathing_techniques):\n                technique = combat.player.breathing_techniques[technique_index]\n                stamina_cost = technique.get('stamina_cost', 20)\n                \n                if combat.player.use_stamina(stamina_cost):\n                    damage_mult = technique.get('damage_multiplier', 1.2)\n                    damage = self.calculate_damage(combat.player.atk, combat.demon.get_defense_value(), damage_mult)\n                    combat.demon.take_damage(damage)\n                    result['player_action'] = f\"🔥 {technique['name']}! **{damage}** dégâts infligés (Stamina: -{stamina_cost})\"\n                    combat.add_log(f\"{combat.player.username} a utilisé {technique['name']} ({damage} dmg)\")\n                else:\n                    # Pas assez de stamina, attaque basique\n                    damage = self.calculate_damage(combat.player.atk, combat.demon.get_defense_value(), 0.7)\n                    combat.demon.take_damage(damage)\n                    result['player_action'] = f\"⚠️ Pas assez de Stamina! Attaque faible: **{damage}** dégâts\"\n                    combat.add_log(f\"{combat.player.username} n'avait pas assez de stamina\")\n            else:\n                # Technique invalide, attaque basique\n                damage = self.calculate_damage(combat.player.atk, combat.demon.get_defense_value())\n                combat.demon.take_damage(damage)\n                result['player_action'] = f\"⚔️ Attaque Basique (Technique invalide): **{damage}** dégâts\"\n        \n        elif action == CombatAction.DEFEND:\n            combat.player_defended = True\n            # Restaurer stamina\n            combat.player.restore_stamina(20)\n            result['player_action'] = \"🛡️ Défense! Stamina restaurée (+20)\"\n            combat.add_log(f\"{combat.player.username} s'est défendu et a restauré la stamina\")\n        \n        elif action == CombatAction.ESCAPE:\n            # Tenter de s'échapper (50% de chance)\n            if random.random() < 0.5:\n                result['player_action'] = \"🏃 Fuite réussie!\"\n                result['escaped'] = True\n                return True, result\n            else:\n                result['player_action'] = \"🏃 Tentative de fuite échouée!\"\n        \n        # Vérifier si le démon est vaincu\n        is_over, status = combat.is_combat_over()\n        if is_over:\n            return True, result | {'combat_status': status}\n        \n        # Action du démon (IA simple)\n        demon_action = self.demon_ai(combat)\n        \n        if demon_action == \"attack\":\n            damage = self.calculate_damage(combat.demon.atk, combat.player.def_ if not combat.player_defended else combat.player.def_ * 1.5)\n            if combat.player_defended:\n                damage = int(damage * 0.5)  # Dégâts réduits si défense\n            \n            combat.player.take_damage(damage)\n            result['demon_action'] = f\"💥 {combat.demon.name} attaque! **{damage}** dégâts pris\"\n            combat.add_log(f\"{combat.demon.name} a attaqué ({damage} dmg)\")\n        \n        elif demon_action == \"defend\":\n            combat.demon_defended = True\n            result['demon_action'] = f\"🛡️ {combat.demon.name} se défend!\"\n            combat.add_log(f\"{combat.demon.name} s'est défendu\")\n        \n        # Restaurer stamina du joueur légèrement\n        if not action == CombatAction.DEFEND:\n            combat.player.restore_stamina(5)\n        \n        result['player_hp'] = combat.player.hp\n        result['demon_hp'] = combat.demon.hp\n        \n        # Vérifier fin du combat\n        is_over, status = combat.is_combat_over()\n        if is_over:\n            result['combat_status'] = status\n            return True, result\n        \n        return False, result\n    \n    def demon_ai(self, combat: CombatState) -> str:\n        \"\"\"IA simple pour le démon\"\"\"\n        hp_percent = combat.demon.hp / combat.demon.max_hp\n        \n        # Si la santé est faible, défendre plus souvent\n        if hp_percent < 0.3:\n            return \"defend\" if random.random() < 0.6 else \"attack\"\n        elif hp_percent < 0.5:\n            return \"defend\" if random.random() < 0.4 else \"attack\"\n        else:\n            return \"attack\" if random.random() < 0.7 else \"defend\"\n    \n    def calculate_damage(self, attacker_atk: int, defender_def: int, action_multiplier: float = 1.0) -> int:\n        \"\"\"Calculer les dégâts\"\"\"\n        base_damage = max(1, attacker_atk - (defender_def // 2))\n        variance = random.randint(-3, 3)\n        final_damage = int((base_damage + variance) * action_multiplier)\n        return max(1, final_damage)\n    \n    def get_combat_rewards(self, combat: CombatState) -> dict:\n        \"\"\"Obtenir les récompenses après le combat\"\"\"\n        # Bonus si le joueur a bien combattu\n        damage_dealt = combat.demon_hp_start - combat.demon.hp\n        damage_ratio = damage_dealt / combat.demon_hp_start if combat.demon_hp_start > 0 else 0\n        \n        # Bonus de performance\n        performance_bonus = 1.0\n        if damage_ratio > 0.8:  # Beaucoup de dégâts infligés\n            performance_bonus = 1.2\n        elif damage_ratio < 0.3:  # Peu de dégâts\n            performance_bonus = 0.8\n        \n        # Bonus de survie\n        survival_bonus = 1.0\n        hp_ratio = combat.player.hp / combat.player_hp_start if combat.player_hp_start > 0 else 0\n        if hp_ratio > 0.7:\n            survival_bonus = 1.1\n        \n        final_xp = int(combat.demon.xp_reward * performance_bonus * survival_bonus)\n        final_gold = int(combat.demon.gold_reward * performance_bonus * survival_bonus)\n        \n        return {\n            'xp': final_xp,\n            'gold': final_gold,\n            'demon': combat.demon.to_dict(),\n            'combat_log': combat.get_combat_log(),\n            'performance': {\n                'damage_ratio': f\"{damage_ratio*100:.1f}%\",\n                'survival_ratio': f\"{hp_ratio*100:.1f}%\",\n                'turns_taken': combat.turn\n            }\n        }\n